{
  "name": "news-fitness-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A2A agent that scores whether an article works as news: title, lead, readability, length.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "start": "node src/server.mjs"
  },
  "dependencies": {
    "@a2a-js/sdk": "^1.1.0",
    "express": "^5.2.1"
  }
}
