# Optional human narration and shot list

**Not recorded. No audio file exists.** The team requested a caption-only review after saying a narrator
was unavailable. This script is retained for a real human if circumstances change; never synthesize it.
Approximately 390 words: allow about three minutes at a clear pace, then measure the actual recording.

| Time | Real shot / evidence | Human narration |
|---|---|---|
| 0:00–0:25 | Public explorer, including its empty current view; local catalogue evidence | “Ainize lets people prepare knowledge for a shared model. Our ETHOnline entry extends an existing product, so the marketplace and model infrastructure are reused foundations. The feature we are showing is a path from blockchain data to teachable inputs with provenance. This explorer is real. Its current view is empty, and the two recorded DART anchors are rejected, not verified products for sale.” |
| 0:25–0:50 | Actual base-response JSON and recorded Graph connection output | “The existing application can answer a real question using its base model. That proves a running model response, not a benefit from our new Graph integration. Now we turn to the new data workflow. This is recorded output from an actual run against The Graph's hosted Subgraph MCP. The complete requests and responses are included in the repository.” |
| 0:50–1:15 | Discovery and schema sections in `graph-run.txt` | “We search for Uniswap subgraphs and inspect the schema before querying. The provider returned twenty-two candidates, but the query-count ranking signal was zero throughout. We therefore named the subgraph explicitly instead of pretending to select the most popular one. The query uses a specific Ethereum block, so its answers carry a clear point in time.” |
| 1:15–1:45 | Actual rows and provenance; highlight hash | “At block twenty-five million, nine hundred sixty-nine thousand and three, the pipeline returned three token records and transformed them into three question-and-answer rows. Each row records its source, query and block. The dataset also has a content hash. These reusable mappings prepare inputs for an AI teaching workflow. This run stopped before upload and training, so we are not claiming a newly trained model or an accuracy improvement.” |
| 1:45–2:15 | Local ENS source/tests and explicit pending-deployment status | “The second integration gives knowledge a naming path. ENS is intended to resolve a human-readable name into a node and a patch, with lineage expressed through the namespace. Security fixes and local tests are useful evidence, but they do not prove a deployment. Sepolia deployment and functional resolution remain pending in this recording. We do not substitute local names or hardcoded values for that proof.” |
| 2:15–3:00 | Root judge entry, continuity history, source snapshots and evidence gates | “Judges can inspect the source snapshots, original commit provenance and recorded evidence from this submission repository. We have separated prior infrastructure, event additions and incomplete work. AI assistance is disclosed alongside the task specification. Our Graph contribution is the auditable bridge from a real provider to teaching inputs. The next steps are a clean rerun from the final source and verified ENS testnet behavior. This demonstration makes the stopping points visible, so a prepared dataset is never mistaken for a trained and verified product.” |

If ENS proof arrives, replace its pending paragraph only with actions visible in the replacement footage.
Do not read future tense as a completed feature. No before/after or statistical latency claim is needed.
