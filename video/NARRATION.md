# Optional human narration and shot list

**Not recorded. No audio file exists.** The team requested a caption-only review after saying a narrator
was unavailable. This script is retained for a real human if circumstances change; never synthesize it.
Approximately 390 words: allow about three minutes at a clear pace, then measure the actual recording.

| Time | Real shot / evidence | Human narration |
|---|---|---|
| 0:00–0:25 | Actual local demo and fetch button | “Ainize lets people prepare knowledge for a shared model. Our ETHOnline entry extends an existing product, so the marketplace and model infrastructure are reused foundations. The new feature is a path from blockchain data to teachable inputs with provenance. This is the real application. Clicking fetch runs our imported Graph integration against a live provider, rather than loading a fixture.” |
| 0:25–0:50 | Live returned rows, source block and hash | “The Graph supplies the token identities. Our client checks the schema and pins its query to an observed Ethereum block. The complete provider requests and responses are included in the repository. The application displays twenty question-and-answer rows, together with their source block and dataset hash. Those are teaching inputs; creating them does not mean a model has already learned them.” |
| 0:50–1:15 | Actual upload button and receipt | “Now the upload button sends those exact reviewed rows to the running Ainize node, using a teaching identity held only by the local server. The node accepts all twenty and returns a dataset identifier. Its stored SHA-256 matches the predicted content hash. That gives the next stage a checkable input. Training and publication are separate actions, and neither was requested in this run.” |
| 1:15–1:45 | Recorded real model comparison | “Here is a separate real response comparison using an existing DART patch. Both answers contain the expected address according to the team's review. In this one observation, the patched answer is shorter. We retain the actual token counts and latencies, but do not call that a statistical speedup or an improvement from the new Graph data. The patch remains rejected in the public catalogue: a live test is not verification for sale.” |
| 1:45–2:15 | Actual ENS permission button against deployed Sepolia resolver | “The second integration uses ENSv2 on Sepolia. Its namespace records the node, the existing patch and Graph dataset provenance. Here we check the deployed resolver's permissions. The same public account is allowed to edit the node record but refused permission to edit the patch record. These are fresh read-only calls against actual deployed contracts. Administrators can explicitly change the rights; the check itself sends no transaction.” |
| 2:15–3:00 | Root judge entry, continuity history, source snapshots and evidence gates | “Judges can inspect the source snapshots, original commit provenance and recorded evidence from this submission repository. We have separated prior infrastructure, event additions and incomplete work. AI assistance is disclosed alongside the task specification. Our Graph contribution is the auditable bridge from a real provider to teaching inputs. The next steps are a clean rerun from the final source and verified ENS testnet behavior. This demonstration makes the stopping points visible, so a prepared dataset is never mistaken for a trained and verified product.” |

If canonical ENS resolution proof arrives, add only actions visible in replacement footage.
Do not read future tense as a completed feature. No before/after or statistical latency claim is needed.
